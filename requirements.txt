# LINE AI Bot — WebUI 控制台所需套件
nicegui==2.7.0
requests>=2.32.0
python-dotenv==1.0.1
# NiceGUI 的 session storage 需要 itsdangerous
itsdangerous>=2.0

# 註 1：對外穿透使用 Cloudflare Tunnel（cloudflared）。cloudflared 是獨立執行檔、
#       不透過 pip 安裝，請用 winget install --id Cloudflare.cloudflared（或官方安裝包）。
#       WebUI 內建安裝精靈會引導完成 login / create / route dns。
# 註 2：LINE 簽章驗證與回覆改用 requests + hmac 自行實作，不綁定特定 SDK 版本。
