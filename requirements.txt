# WebUI 控制台（webui.py）所需套件
nicegui==2.7.0
pyngrok==7.2.0
requests>=2.32.0
python-dotenv==1.0.1

# 註：webui.py 不依賴 line-bot-sdk，改用 requests + hmac 自行處理 LINE 簽章與回覆。
# 若仍要使用舊版純 CLI 的 app.py，需另外安裝 flask 與 line-bot-sdk==3.11.0。
